{
  "name": "habitual",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install-client": "npm install --prefix client",
    "install-server": "npm install --prefix server",
    "install-all": "npm run install-client && npm install-server",
    "client": "npm run dev --prefix client",
    "server": "npm run dev --prefix server",
    "start": "cross-env NODE_ENV=production npm run start --prefix server",
    "dev": "cross-env NODE_ENV=development npm run dev --prefix server",
    "old-dev": "npm run dev --prefix server & npm run dev --prefix client",
    "deploy": "npm run build --prefix client && npm start --prefix server",
    "test": "npm run test --prefix server && npm run test --prefix client"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/brittneypostma/habitual-app.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/brittneypostma/habitual-app/issues"
  },
  "homepage": "https://github.com/brittneypostma/habitual-app#readme",
  "devDependencies": {
    "cross-env": "^7.0.3"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "dotenv": "^10.0.0",
    "helmet": "^4.6.0",
    "mongodb": "^3.6.9",
    "pm2": "^4.5.6"
  }
}
